<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Autenticação de Usuário</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/particles.js/2.0.0/particles.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/notyf@3/notyf.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/notyf@3/notyf.min.css" />
    <style>
        .fade-in {
            animation: fadeIn 0.5s ease-in;
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center overflow-hidden" style="background-color: #d7282f;">
    <div id="particles-js" class="absolute inset-0"></div>

    <div class="relative z-10 w-full max-w-2xl px-4">
        <div id="formLoginContainer" class="rounded-3xl shadow-2xl p-12 fade-in" style="background-color: #e5e7eb;">
            <div class="text-center mb-10">
                <div class="inline-flex items-center justify-center w-20 h-20 rounded-full mb-6 shadow-lg" style="background-color: #d7282f;">
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
                    </svg>
                </div>
                <h1 class="text-4xl font-bold text-gray-800 mb-3">Bem-vindo</h1>
                <p class="text-gray-600 text-lg">Faça login para continuar</p>
            </div>

            <form id="formLogin" class="space-y-8">
                <div>
                    <label for="loginEmail" class="block text-sm font-semibold text-gray-800 mb-3">E-mail</label>
                    <input type="email" id="loginEmail" name="email" required
                           class="w-full px-6 py-4 bg-white border border-gray-300 rounded-xl text-gray-800 placeholder-gray-400 focus:ring-2 focus:border-transparent transition duration-300 outline-none text-lg"
                           placeholder="seu@email.com">
                </div>

                <div>
                    <label for="loginSenha" class="block text-sm font-semibold text-gray-800 mb-3">Senha</label>
                    <input type="password" id="loginSenha" name="senha" required
                           class="w-full px-6 py-4 bg-white border border-gray-300 rounded-xl text-gray-800 placeholder-gray-400 focus:ring-2 focus:border-transparent transition duration-300 outline-none text-lg"
                           placeholder="••••••••">
                </div>



                <button type="submit"
                        class="w-full text-white py-4 rounded-xl font-bold text-lg hover:opacity-90 focus:ring-4 transition duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
                        style="background-color: #d7282f;">
                    Entrar
                </button>
            </form>

            <div id="mensagemLogin" class="mt-6 p-4 rounded-xl hidden"></div>

            <div class="mt-8 text-center">
                <p class="text-gray-600">
                    Não tem uma conta? <a href="#" id="btnMostrarCadastro" class="font-semibold hover:opacity-80 transition duration-200" style="color: #d7282f;">Cadastre-se</a>
                </p>
            </div>
        </div>

        <div id="formCadastroContainer" class="rounded-3xl shadow-2xl p-12 hidden fade-in" style="background-color: #e5e7eb;">
            <div class="text-center mb-10">
                <div class="inline-flex items-center justify-center w-20 h-20 rounded-full mb-6 shadow-lg" style="background-color: #d7282f;">
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                    </svg>
                </div>
                <h1 class="text-4xl font-bold text-gray-800 mb-3">Criar Conta</h1>
                <p class="text-gray-600 text-lg">Preencha seus dados para começar</p>
            </div>

            <form id="formCadastro" class="space-y-8">
                <div>
                    <label for="cadastroEmail" class="block text-sm font-semibold text-gray-800 mb-3">E-mail</label>
                    <input type="email" id="cadastroEmail" name="email" required
                           class="w-full px-6 py-4 bg-white border border-gray-300 rounded-xl text-gray-800 placeholder-gray-400 focus:ring-2 focus:border-transparent transition duration-300 outline-none text-lg"
                           placeholder="seu@email.com">
                </div>

                <div>
                    <label for="cadastroSenha" class="block text-sm font-semibold text-gray-800 mb-3">Senha</label>
                    <input type="password" id="cadastroSenha" name="senha" required
                           class="w-full px-6 py-4 bg-white border border-gray-300 rounded-xl text-gray-800 placeholder-gray-400 focus:ring-2 focus:border-transparent transition duration-300 outline-none text-lg"
                           placeholder="Digite sua senha">
                </div>

                <button type="submit"
                        class="w-full text-white py-4 rounded-xl font-bold text-lg hover:opacity-90 focus:ring-4 transition duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
                        style="background-color: #d7282f;">
                    Cadastrar
                </button>
            </form>

            <div id="mensagemCadastro" class="mt-6 p-4 rounded-xl hidden"></div>

            <div class="mt-8 text-center">
                <p class="text-gray-600">
                    Já tem uma conta? <a href="#" id="btnMostrarLogin" class="font-semibold hover:opacity-80 transition duration-200" style="color: #d7282f;">Faça login</a>
                </p>
            </div>
        </div>
    </div>

    <script>
        
        particlesJS('particles-js', {
            particles: {
                number: {
                    value: 50,
                    density: {
                        enable: true,
                        value_area: 800
                    }
                },
                color: {
                    value: '#ffffff'
                },
                shape: {
                    type: 'circle',
                },
                opacity: {
                    value: 0.5,
                    random: false,
                },
                size: {
                    value: 3,
                    random: true,
                },
                line_linked: {
                    enable: true,
                    distance: 150,
                    color: '#ffffff',
                    opacity: 0.4,
                    width: 1
                },
                move: {
                    enable: true,
                    speed: 2,
                    direction: 'none',
                    random: false,
                    straight: false,
                    out_mode: 'out',
                    bounce: false,
                }
            },
            interactivity: {
                detect_on: 'canvas',
                events: {
                    onhover: {
                        enable: true,
                        mode: 'grab'
                    },
                    onclick: {
                        enable: true,
                        mode: 'remove'
                    },
                    resize: true
                },
                modes: {
                    repulse: {
                        distance: 100,
                        duration: 0.4
                    },
                    push: {
                        particles_nb: 4
                    }
                }
            },
            retina_detect: true
        });

        document.getElementById('btnMostrarLogin').addEventListener('click', function(e) {
            e.preventDefault();
            const cadastroContainer = document.getElementById('formCadastroContainer');
            const loginContainer = document.getElementById('formLoginContainer');

            cadastroContainer.classList.add('hidden');
            loginContainer.classList.remove('hidden');
            loginContainer.classList.add('fade-in');
        });

        document.getElementById('btnMostrarCadastro').addEventListener('click', function(e) {
            e.preventDefault();
            const loginContainer = document.getElementById('formLoginContainer');
            const cadastroContainer = document.getElementById('formCadastroContainer');

            loginContainer.classList.add('hidden');
            cadastroContainer.classList.remove('hidden');
            cadastroContainer.classList.add('fade-in');
        });

        document.getElementById('formCadastro').addEventListener('submit', function(e) {
            e.preventDefault();

            const submitButton = this.querySelector('button[type="submit"]');
            if (submitButton.disabled) {
                return; 
            }
            
            submitButton.disabled = true;
            submitButton.textContent = 'Processando...';
            submitButton.classList.add('opacity-75', 'cursor-not-allowed');

            const mensagemDiv = document.getElementById('mensagemCadastro');

            const requestData = {
                email: document.getElementById('cadastroEmail').value,
                senha: document.getElementById('cadastroSenha').value
            };

            $.ajax({
                url: "@Url.Action("CadastrarUsuario", "Login")",
                type: "POST",
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                data: JSON.stringify(requestData),
                headers: {
                    'RequestVerificationToken':
                        document.querySelector('input[name="__RequestVerificationToken"]')?.value
                },
                success: function (response) {
                    if (response.success) {
                        const notyf = new Notyf({
                            duration: 3000,
                            position: { x: 'right', y: 'top' }
                        });
                        notyf.success('Cadastro realizado com sucesso! Redirecionando para o login...');
                        
                        document.getElementById('formCadastro').reset();
                        
                        setTimeout(() => {
                            document.getElementById('btnMostrarLogin').click();
                        }, 2000);
                    } else {
                        const notyf = new Notyf({
                            duration: 5000,
                            position: { x: 'right', y: 'top' }
                        });
                        if (response.errors && response.errors.length > 0) {
                            notyf.error(response.errors.join(', '));
                        } else {
                            notyf.error(response.message);
                        }
                        
                        submitButton.disabled = false;
                        submitButton.textContent = 'Cadastrar';
                        submitButton.classList.remove('opacity-75', 'cursor-not-allowed');
                    }
                },
                error: function (xhr) {
                    mensagemDiv.className = 'mt-6 p-4 rounded-xl bg-red-100 border border-red-400 text-red-700';
                    mensagemDiv.textContent = 'Erro ao cadastrar usuário (HTTP ' + xhr.status + ')';
                    mensagemDiv.classList.remove('hidden');
                    console.error('Erro:', xhr);
                    
                    submitButton.disabled = false;
                    submitButton.textContent = 'Cadastrar';
                    submitButton.classList.remove('opacity-75', 'cursor-not-allowed');
                }
            });

            setTimeout(() => {
                mensagemDiv.classList.add('hidden');
            }, 8000);
        });

        document.getElementById('formLogin').addEventListener('submit', function(e) {
            e.preventDefault();

            const submitButton = this.querySelector('button[type="submit"]');
            if (submitButton.disabled) {
                return; 
            }
            
            submitButton.disabled = true;
            submitButton.textContent = 'Processando...';
            submitButton.classList.add('opacity-75', 'cursor-not-allowed');

            const mensagemDiv = document.getElementById('mensagemLogin');

            const dados = {
                email: document.getElementById('loginEmail').value,
                senha: document.getElementById('loginSenha').value
            };

            $.ajax({
                url: "@Url.Action("Login", "Login")",
                type: "POST",
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                data: JSON.stringify(dados),
                headers: {
                    'RequestVerificationToken':
                        document.querySelector('input[name="__RequestVerificationToken"]')?.value
                },
                success: function (response) {
                    if (response.success) {
                        const notyf = new Notyf({
                            duration: 3000,
                            position: { x: 'right', y: 'top' }
                        });
                        notyf.success('Login realizado com sucesso! Redirecionando...');
                                                
                        setTimeout(() => {
                            window.location.href = '/Home';
                        }, 1500);
                    } else {
                        const notyf = new Notyf({
                            duration: 5000,
                            position: { x: 'right', y: 'top' }
                        });
                        if (response.errors && response.errors.length > 0) {
                            notyf.error(response.errors.join(', '));
                        } else {
                            notyf.error(response.message);
                        }
                        
                        submitButton.disabled = false;
                        submitButton.textContent = 'Entrar';
                        submitButton.classList.remove('opacity-75', 'cursor-not-allowed');
                    }
                },
                error: function (xhr) {
                    const notyf = new Notyf({
                        duration: 5000,
                        position: { x: 'right', y: 'top' }
                    });
                    notyf.error('E-mail ou senha Inválidos');
                    console.error('Erro:', xhr);
                    
                    submitButton.disabled = false;
                    submitButton.textContent = 'Entrar';
                    submitButton.classList.remove('opacity-75', 'cursor-not-allowed');
                }
            });

            setTimeout(() => {
                mensagemDiv.classList.add('hidden');
            }, 8000);
        });
    </script>
</body>
</html>